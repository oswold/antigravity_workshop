# AI Pulse — Python Backend Environment Variables

# ── Gemini (cloud LLM) ────────────────────────────────────────────────────────
# Replace this with your actual Gemini API Key (e.g. AQ.zaSy...)
GEMINI_API_KEY=gemini_api_key_from_ai_studio

# ── WhatsApp MCP Bridge ───────────────────────────────────────────────────────
# Path to the cloned lharries/whatsapp-mcp repo (legacy)
# WHATSAPP_MCP_PATH=C:\Users\<user>\whatsapp-mcp

# Path to the root of this project (used for CLI messages)
REPO_PATH=C:\Users\YourName\podcasts

# ── GitHub Pages publish (optional) ──────────────────────────────────────────
GITHUB_OWNER=your-github-username
GITHUB_REPO=ai-pulse-newsletter
GITHUB_PERSONAL_ACCESS_TOKEN=github_pat_your_token_here
# GMAIL
GMAIL_USER=your gmail account
GMAIL_APP_PASSWORD=your gmail app password

# ── Model override (optional) ─────────────────────────────────────────────────
# Set to "ollama" to force local Gemma3 via Ollama
# MODEL=ollama

# Docker MCP Gateway
MCP_GATEWAY_AUTH_TOKEN=your mcp gateway password
